#include <stdio.h>

/**
 * main - The main entry point of a function
 * @void: takes no paremeter
 *
 * Return: after successfully compeleting
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
